# Database configuration (required)
DATABASE_USER=oktopus
DATABASE_PASSWORD=oktopus
# DATABASE_URL=jdbc:postgresql://localhost:5432/oktopus
# DATABASE_MAX_POOL_SIZE=10
# DATABASE_MIN_IDLE=2

# Server configuration
# PORT=8080

# JWT configuration (required)
# JWT_SECRET=your-secret-key-at-least-256-bits

# Mapbox (required for Flutter geocoding, pass via --dart-define=MAPBOX_ACCESS_TOKEN=...)
# MAPBOX_ACCESS_TOKEN=pk.your_mapbox_token_here
