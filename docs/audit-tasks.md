# Задачи аудита проекта

Чеклист направлений аудита. Отмечай выполненные галочкой.

---

## 1. Поиск кода, непокрытого юнит-тестами

- [ ] Найти классы и методы application-слоя (сервисы, валидаторы) и доменную логику без unit-тестов.

Ориентир по уже покрытым областям — наличие in-memory реализаций репозиториев (`InMemoryRideRepository`, `MockPersonRepository` и т.п.) и соответствующих `*Spec`. Цель — список непокрытой бизнес-логики по модулям (core, auth, ride, driver, schedule, notification, billing).

## 2. Поиск кода, непокрытого интеграционными тестами

- [ ] Найти репозитории и HTTP-эндпоинты без интеграционных тестов на Testcontainers.

Проверять Doobie-реализации в `infrastructure/repository/` и route-хендлеры. Интеграционные тесты должны идти против реального PostgreSQL (Testcontainers), без моков БД. Цель — список репозиториев/эндпоинтов без интеграционного покрытия.

## 3. Поиск уязвимостей

- [ ] Найти нарушения безопасности.

Что искать: нарушения tenant-изоляции (фильтрация по `CompanyId` в каждом запросе, включая SQL-слой — `findById`/`delete`/`update`), утечки чувствительных данных в логах, SQL/CSV-инъекции, хардкод секретов, слабую обработку JWT. Цель — список уязвимостей с указанием места и серьёзности.

## 4. Поиск нарушений SOLID

- [ ] Найти нарушения принципов SOLID.

Что искать: бизнес-логику в route-хендлерах (должна быть в application-слое), перегруженные «god»-сервисы, нарушение разделения слоёв domain/application/infrastructure, неверные зависимости между слоями. Цель — список нарушений с указанием файла и принципа.

---

## Найденные баги — 2026-06-19

> Проверено по коду (компиляция OK, 1147 тестов зелёные). Severity: HIGH / MEDIUM / LOW.
> Полный отчёт с обоснованиями: `docs/audit-report-2026-06-19.md`.

- [ ] **[HIGH] `getRidesForUser` теряет поездки у мультироль-пользователей** — `ride/src/main/scala/com/shevchyk/ride/application/service/RideService.scala:170` — `findByClientId(userId).orElse(findByDriverId(userId))`: `.orElse` отдаёт driver-поездки только если client-запрос УПАЛ, а не объединяет списки. Для диспетчера-водителя driver-поездки молча пропадают. Фикс — объединить списки + `distinctBy(_.id)`. _Категория: Functional._
- [ ] **[HIGH] `assignDriver` не валидирует ScheduleDay** — `ride/src/main/scala/com/shevchyk/ride/application/service/RideService.scala` (метод assignDriver) — заявленное правило «назначение должно ссылаться на валидный ScheduleDay» (CLAUDE.md constraint #3) не реализовано: поездку с `scheduleDayId` можно назначить, даже если день отсутствует/отменён/чужой. _Категория: Functional._
- [ ] **[HIGH] ChatService на `Task` + `RuntimeException`** — `ride/src/main/scala/com/shevchyk/ride/application/service/ChatService.scala:10-12` — нарушает «ZIO typed, no throw»: `sendMessage/getMessages: Task[...]`, ошибки кидаются как `new RuntimeException(...)`. Нужен `sealed trait ChatError` + `IO[ChatError, _]`. _Категория: SOLID._
- [ ] **[MEDIUM] `InvoiceStatus.fromString` молча → `Draft`** — `billing/src/main/scala/com/shevchyk/billing/domain/Invoice.scala` (`case _ => Draft`), используется при чтении из БД `billing/.../repository/PostgresInvoiceRepository.scala:82`. Неизвестный/повреждённый статус читается как черновик (оплаченный счёт → Draft). Фикс — Option/Either + явный fail. _Категория: Functional._
- [ ] **[MEDIUM] Неисчерпывающий match по `PersonRole`** — `ride/src/main/scala/com/shevchyk/ride/application/service/RideService.scala` (`validateCancelPermission`) — нет `case _`; новая роль даст MatchError в рантайме. Добавить явный deny-`case _`. _Категория: Functional._
- [ ] **[MEDIUM] Ошибки уведомлений/аудита глушатся при назначении** — `ride/src/main/scala/com/shevchyk/ride/application/service/RideService.scala` (assignDriver) — `eventHub.publish(...).ignore` (без tapError), `emailSmsService.send...().ignore`, `auditService.log(...).ignore`: водитель может не узнать о назначении, нет алерта. Добавить единообразный `.tapError(logWarning)`. _Категория: Functional._
- [ ] **[MEDIUM] Пагинация не валидирует отрицательные offset/limit** — `ride/src/main/scala/com/shevchyk/ride/openapi/RideApi.scala` (listRides) — `limit=-1`/`offset=-10` уходят в SQL без проверки. Клампить (limit 1..200, offset ≥ 0). _Категория: Functional._
- [ ] **[MEDIUM] RideService — god-object (SRP/ISP)** — `ride/src/main/scala/com/shevchyk/ride/application/service/RideService.scala` — trait ~27 методов / ~786 строк (CRUD + статус-машина + статистика + платежи + company-запросы); `RideRepository` ~29 методов. Выделить `RideStatsService`/`RideStatsRepository` и платежи. _Категория: SOLID._
- [ ] **[MEDIUM] Дублирование инфраструктуры между модулями** — `Validator` trait продублирован (`ride/.../validation/Validator.scala` и `schedule/.../validation/Validator.scala`); два расходящихся `ClientCompanyRepository` (core vs billing); DATEV CSV скопирован в `ride/.../openapi/ExportApi.scala`; `checkRole`/`requireCompanyId` переопределены в `RideSecure` и `UserApi`. Вынести общее в `core`. _Категория: SOLID._
- [ ] **[MEDIUM] Бизнес-логика в HTTP-хендлерах** — `ride/src/main/scala/com/shevchyk/ride/openapi/RideApi.scala` (createRide) — override clientId по роли и валидация рейтинга 1..5 в хендлере вместо application/validator-слоя. _Категория: SOLID._
- [ ] **[LOW] Self-data чтения без company-фильтра (defense-in-depth)** — `api/src/main/scala/com/shevchyk/app/openapi/GdprApi.scala` (export) и `ride/src/main/scala/com/shevchyk/ride/openapi/ExpenseApi.scala:83` — `findByClientId/findByDriverId(user.userId)` без company-фильтра. НЕ эксплуатируется (один PersonId = одна компания), но перейти на `*AndCompany`-варианты. _Категория: Security._
- [ ] **[LOW] Неотфильтрованные repo-методы провоцируют будущие tenant-баги** — `ride/.../repository/PostgresRideRepository.scala` (`findAll/findByClientId/findByDriverId`) и `PostgresExpenseRepository.findByDriverId` — нет `company_id` в WHERE. Сейчас не утечка (зовут по своему id), но ловушка. Добавить фильтр или задепрекейтить в пользу `*AndCompany`. _Категория: Security._
- [ ] **[LOW] `PaymentChecker` — постоянный мок** — `billing/src/main/scala/com/shevchyk/billing/application/PaymentChecker.scala` — `isPaid` всегда `false` по дизайну; авто-сверка с банком не происходит, только ручной `markPaid`. Известно/намеренно, зафиксировано чтобы не приняли за баг. _Категория: Functional._

---

## Найденные баги — конкурентность/деньги — 2026-06-20

> Проверено по коду (находки агентов отфильтрованы; ложные срабатывания исключены — повтор-отмены fee, таймзона isInThePast). Severity: HIGH / MEDIUM / LOW.

- [x] **[MEDIUM] Отрицательный `fee` при отмене не валидируется** — `ride/src/main/scala/com/shevchyk/ride/domain/RideDomain.scala:185` (`CancelRideRequest.fee: Option[BigDecimal]`) + `ride/.../openapi/RideApi.scala:484-495` (`cancelRideServer` не вызывает `.validate`, передаёт `fee` напрямую) — диспетчер может передать `fee = -100` → отрицательная «комиссия» = по сути рефанд клиенту. Прямо влияет на деньги. Фикс — валидатор `CancelRideApiRequest` с проверкой `fee >= 0` (по аналогии с `validatePrice` в `RideValidators`). _Категория: Functional._ — **ИСПРАВЛЕНО** (master 79776a3): `cancelRideApiRequestValidator` + `.validate` в хендлере + гард в `cancelRideWithReason`; тесты валидатора и сервиса.
- [ ] **[MEDIUM] `markPayment` перетирает зафиксированный способ оплаты** — `ride/src/main/scala/com/shevchyk/ride/application/service/RideService.scala:614-641` — `paymentMethod.orElse(ride.paymentMethod)` (632-633): повторный вызов с `Some(Card)` перетрёт уже сохранённый `Cash`; нет гарда «способ оплаты уже зафиксирован для оплаченной поездки». Важно для DATEV/бухгалтерии. Фикс — не менять `paymentMethod`/`paidAt` если ride уже `Paid`. _Категория: Functional._
- [ ] **[MEDIUM] TOCTOU при cancel/start/complete (безусловный `update`)** — `ride/src/main/scala/com/shevchyk/ride/application/service/RideService.scala` — `cancelRide:225-236`, `cancelRideWithReason:246-265`, `startRide:~205`, `completeRide:210-220` читают ride, проверяют статус, затем делают **безусловный** `rideRepository.update(...)`. Между read и update конкурентный переход (напр. → Completed) перетирается. Контраст с `assignDriver`/`reassignDriver`, где есть атомарный `updateIfStatus`. Фикс — перевести на `updateIfStatus(updated, Set(допустимые исходные статусы))`. _Категория: Functional._
- [ ] **[LOW] TOCTOU проверки конфликта расписания** — `ride/src/main/scala/com/shevchyk/ride/application/service/RideService.scala:731-753` (`checkScheduleConflict`) — читает активные поездки водителя ДО атомарного `updateIfStatus` в `assignDriver:445`/`reassignDriver:543`. `updateIfStatus` гейтит только статус назначаемой поездки, не пересечение расписания: два диспетчера могут одновременно назначить одного водителя на пересекающиеся слоты, оба пройдут. Узкое окно. Фикс — SQL-level exclusion constraint или проверка внутри транзакции назначения. _Категория: Functional._
- [ ] **[LOW] TOCTOU blacklist при reassign** — `ride/src/main/scala/com/shevchyk/ride/application/service/RideService.scala:539-550` (`reassignDriver`) — проверка `isBlacklisted` (539) выполняется до `updateIfStatus` (550), который blacklist не гейтит. Если водителя добавят в blacklist ровно между проверкой и записью — reassign всё равно пройдёт. Очень узкое окно, низкий импакт. _Категория: Functional._

---

## Бизнес-нестыковки (требования ↔ реализация) — 2026-06-21

> Аудит против `docs/requirements.md` + инвариантов CLAUDE.md. Помечено, что проверено лично по коду, а что — находка агента (требует доперепроверки перед фиксом). Дедуп с разделами выше: ScheduleDay-валидация уже зафиксирована (см. `[HIGH] assignDriver не валидирует ScheduleDay`); пункт по мультироли ниже шире, чем `getRidesForUser` (стр. с `[HIGH] getRidesForUser теряет поездки`).

- [ ] **[HIGH] Мультироль игнорируется во всех Tapir secure-слоях, кроме `AppSecure`** _(проверено лично)_ — `ride/.../openapi/RideSecure.scala:38-61`, `driver/.../openapi/DriverApi.scala:75-99`, `schedule/.../openapi/ScheduleApi.scala:58-79` (`requireDispatcherOrAdmin`), `api/.../openapi/UserApi.scala:93-104` — строят `AuthenticatedUser` без `roles` и проверяют права только по primary-роли (`user.role`), хотя JWT несёт `roles` и эталон `api/.../openapi/AppSecure.scala:15-65` использует `effectiveRoles`. Заявлено «диспетчер может быть водителем» (`Person.roles: Set[PersonRole]`). Эффект: dispatcher-as-driver (primary=Dispatcher) получает 403 на driver-эндпоинтах поездок/расписания и не может вести свои поездки как водитель. Системная причина — копия `checkRole` без `effectiveRoles`. _Категория: Functional/Security._
- [ ] **[HIGH] Цена и оценка теряются при создании поездки** _(проверено лично)_ — `ride/.../infrastructure/http/dto/RideApiModels.scala:99` (`CreateRideApiRequest.price`) + `toDomain` `:351-380` не переносит `price`; `CreateRideRequest` (`ride/.../domain/RideDomain.scala:229-239`) поля цены не имеет; `RideService.createRide` не вызывает `RideEstimateService`. Поездка всегда создаётся с `estimatedPrice=None`/`finalPrice=None`. Эффект: `/estimate` показывает клиенту цену, но в поездке она не сохраняется → vehicle-class multiplier и ночная надбавка не доходят до биллинга, счёт без цены/надбавок. _Категория: Functional._
- [x] **[HIGH] Противоречие Netto/Brutto: счёт vs квитанция** _(проверено лично)_ — `billing/.../application/InvoiceService.scala:45` (комментарий «ride price treated as gross/Brutto») против `recalculate` `:361-366`, где `ride.price` → `item.total` → `subtotal` трактуется как Netto и НДС накручивается сверху (`tax = subtotal*rate/100`, `total = subtotal+tax`); при этом квитанция `billing/.../application/PdfGenerator.scala:245-247` трактует ту же цену как Brutto (`net = gross/(1+rate)`). Одна цена → в счёте завышенный итог и неверный MwSt (юридически значимый документ, DATEV). _Категория: Functional._ — **ИСПРАВЛЕНО** (master 99b9413, ревью PASS): `recalculate` выводит Netto/MwSt из Brutto (`net = gross/(1+rate)`, `tax = gross-net`, `total = gross`), согласовано с receipt-путём; обновлён `InvoiceServiceSpec` (Brutto-семантика + явный кейс 119/19→100 на изолированном февральском периоде).
- [ ] **[MEDIUM] Лимит поездок на водителя в день не реализован** _(находка агента — доперепроверить)_ — `ride/.../application/service/RideService.scala:447-602` (`assignDriver`/`reassignDriver`) — Business Rule #5 «Respect maximum rides per driver per day» не проверяется при назначении. _Категория: Functional._
- [ ] **[MEDIUM] Travel time захардкожен 60 мин вместо routing-API** _(находка агента — доперепроверить)_ — `ride/.../application/service/RideService.scala:~752-800` (`checkScheduleConflict`/`ridesOverlap`) — `DefaultRideDurationMinutes = 60` для всех поездок независимо от расстояния (покрытие до 100 км). Требование #12 предполагает расчёт через Google/HERE. Недостоверный буфер → риск опозданий при приоритете «client should not wait». _Категория: Functional._
- [x] **[MEDIUM] Таймзона UTC вместо Europe/Berlin в документах/границах суток** _(частично проверено)_ — `billing/.../application/PdfGenerator.scala:183,241` форматирует дату/время квитанции через `ZoneOffset.UTC`; Мюнхен = UTC+1/+2, дата/время в Quittung смещается на 1-2 часа. Тот же риск для границ суток ночной надбавки и «today's rides». _Категория: Functional._ — ИСПРАВЛЕНО частично: PDF (agent/fix-pdf-timezone)
- [ ] **[MEDIUM] Фиксированные тарифы по клиент-компании не реализованы** _(находка агента — доперепроверить)_ — ride-модуль считает цену только distance/time-based; логики fixed-price по `clientCompanyId` нет. Требование #9 «agreements with companies, price is fixed». _Категория: Functional._
- [ ] **[LOW] `cancellationFee` не применяется автоматически; waiting fee отсутствует** _(находка агента)_ — `cancellationFee` сохраняется при отмене, но не подтягивается из `core/.../domain/CompanySettings.scala` (`cancellationFeeDefault`) и не попадает в биллинг; платы за ожидание (клиент опоздал 30+ мин) нет вовсе. _Категория: Functional._
- [ ] **[LOW] Несогласованность wire-формата роли в ride/driver-слоях** _(проверено лично)_ — `ride/.../openapi/RideSecure.scala:78-86` (`toPersonRole`) не знает `CLIENT_SECRETARY`/`SUPER_ADMIN` → молча `case _ => Client`; `driver/.../openapi/DriverApi.scala:302` зовёт `PersonRole.valueOf(user.role)` на `.toString`-форме роли — рассогласование форматов сервер↔слой. _Категория: Functional._

---

## Глубокий аудит (security/billing/notification) — 2026-06-21

> Второй проход агентами по auth/core/notification/driver/schedule + конкурентность. Подтверждённые лично отмечены; остальные — доперепроверить. Tenant-фикс assign/reassign и НДС-Brutto уже исправлены (см. выше).

- [x] **[HIGH] Cross-tenant назначение водителя через assign/reassign** _(проверено лично)_ — `ride/.../openapi/RideApi.scala:419-442` — `assignDriverServer`/`reassignDriverServer` не проверяли `ride.companyId == companyId` диспетчера; `RideService.assignDriver` сверяет лишь driver↔ride, не dispatcher↔ride. Диспетчер компании A мог (пере)назначить водителя на поездку компании B. _Категория: Security._ — **ИСПРАВЛЕНО** (master 99b9413, ревью PASS): проверка `existing.companyId != companyId` → `RideNotFound` (404, как в `getRideServer`); регресс-тест `api/.../RideAssignIsolationSpec.scala` (7 кейсов).
- [ ] **[HIGH] Та же cross-tenant дыра в `EmergencyApi.reassignServer`** _(проверено ревьюером)_ — `api/.../openapi/EmergencyApi.scala:55-99` — зовёт `rideService.getRideById` + `reassignDriver` без проверки `ride.companyId == companyId`. Диспетчер компании A может инициировать аварийное переназначение на поездке компании B. Аналог уже закрытого фикса assign/reassign — применить тот же гард. _Категория: Security._
- [ ] **[HIGH] FCM-токены не привязаны к компании** _(находка агента — доперепроверить)_ — `api/.../openapi/UserApi.scala:577-582` (`registerFcmTokenServer`) + `FcmTokenRepository` — токен сохраняется без `company_id`; `findByPersonId` отдаёт все токены персоны вне компании. Риск отправки уведомления из контекста чужой компании; lookup неизолирован. _Категория: Security._
- [ ] **[MEDIUM] TOCTOU в `AuthService.updateUser`/`changePassword`** _(находка агента — доперепроверить)_ — `auth/.../application/AuthService.scala:151` (`updateUser`) и `:201-217` (`changePassword`) — `findById` читает юзера БЕЗ company_id, затем `update` (в SQL есть company_id-гард). Read-modify-write на чужом id: read проходит, write — нет, но это рассинхрон/утечка существования. Перейти на `findByIdAndCompany`. _Категория: Security._
- [ ] **[MEDIUM] SQL-методы PersonRepository без company_id** _(находка агента — доперепроверить, пересекается с разделом 2026-06-19)_ — `core/.../repository/PostgresPersonRepository.scala:216` (`updateLastLogin`), `:249` (`setAvatar`), `:254` (`deleteAvatar`) — UPDATE/SELECT без `AND company_id`. Read/write по чужому id (self-операции сейчас, но defense-in-depth). _Категория: Security._
- [ ] **[MEDIUM] Перекрытие смен расписания не проверяется** _(находка агента — доперепроверить)_ — `schedule/.../application/ScheduleService.scala` (`createScheduleDay`) — валидирует `start<end` и компанию водителя, но не ищет пересекающиеся смены на ту же дату → у водителя могут быть 09:00–17:00 и 10:00–18:00 одновременно. _Категория: Functional._
- [ ] **[MEDIUM] `GeofenceTriggered` только логируется, push диспетчеру не шлётся** _(находка агента — доперепроверить)_ — `notification/.../application/PushNotificationListener.scala` (ветка `GeofenceTriggered`) — событие публикуется, но слушатель лишь `ZIO.logInfo`, без `notifyUser` диспетчерам (в отличие от `EtaAtRisk`). Geofence-алерты теряются. _Категория: Functional._
- [ ] **[LOW] Молчаливое глушение FCM/событий `.ignore`** _(находка агента)_ — `notification/.../FcmService.scala`, `PushNotificationListener` (catchAll→logWarning, send→.ignore) — сбой доставки push логируется, но вызов «успешен»; уведомление не доходит, fallback нет. Перейти на `.tapError` + очередь/ретрай. _Категория: Functional._
- [ ] **[LOW] Дедуп checkpoint-уведомлений неатомарен** _(находка агента — доперепроверить)_ — `notification/.../PushNotificationListener.scala` (`CheckpointReached`) — `isAlreadySent`→`markSent` не атомарно; два конкурентных события могут оба пройти. Использовать `markSentIfNew` (как `EtaAlertRepository`). _Категория: Functional._

---

## Конкурентность: `markPayment` lost-update + регрессия CAS — 2026-06-22

> Контекст: статус-переходы поездки переведены на атомарный CAS (`updateIfStatus`) и смержены в master (`b0ca6c5`, запушено). При закрытии этой задачи всплыли две связанные проблемы вокруг `markPayment`. _Проверено лично по коду master._

- [ ] **[HIGH] Регрессия: CAS-защита `markPayment` потеряна при merge** _(проверено лично)_ — `ride/.../application/service/RideService.scala:635-662` — в master `markPayment` снова на безусловном `rideRepository.update(updatedRide)` без CAS. CAS-по-статусу для `Paid` (`updateIfStatus(_, Set(Completed))`), который был в коммите `9d405eb`, **затёрт версией master при merge `e966b8b`** (обе стороны меняли метод, авто-merge выбрал master-версию; тесты остались зелёными, т.к. concurrent-тест на этот путь не дошёл до master). Эффект: отменённая поездка снова может быть помечена `Paid` гонкой cancel↔markPayment. Нужно вернуть CAS-по-статусу. _Категория: Functional/Concurrency._
- [ ] **[HIGH] Lost-update `markPayment` по payment-полям** _(проверено лично)_ — `ride/.../application/service/RideService.scala:635-662` — read-modify-write: `getRideById` → правка `paymentStatus/paymentMethod/paidAt` в Scala → `update` без условия по полю. Два конкурентных `markPayment(Paid, Cash)` и `markPayment(Paid, Card)` на одной Completed-поездке перезаписывают друг друга (теряется метод/время платежа последнего проигравшего). CAS-по-статусу `{Completed}` это НЕ ловит — оба видят статус Completed. Денежный документ → требует атомарности. _Категория: Functional/Concurrency._

  **Варианты решения (выбрать при реализации):**
  1. **Field-level CAS для `Paid` (рекомендуется, соразмерно).** Новый метод репозитория по образцу `updateCheckpoint` (`PostgresRideRepository.scala:648-670`): `UPDATE rides SET payment_status='Paid', payment_method=?, paid_at=NOW() WHERE id=? AND status='Completed' AND payment_status<>'Paid'`, вернуть `affected_rows>0`. Условие `payment_status<>'Paid'` закрывает lost-update (второй конкурент видит `Paid` → 0 строк → already-paid, идемпотентно) И статус-гонку (`status='Completed'`) одним выстрелом. Pending/Unpaid оставить на обычном `update` (нет денежного риска). Без миграции. In-memory аналог — через `rides.modify` (как `updateIfStatus`).
  2. **Обобщённый `updatePaymentIfStatus(ride, expectedPaymentStatus, expectedRideStatus)`** для Paid/Pending/Unpaid. Полностью атомарно, но больше кода и открытый вопрос «что возвращать проигравшему не-Paid» (ошибка vs idempotent-noop).
  3. **Version-колонка `rides.version` + optimistic lock на все `update`.** Общее решение для всего репозитория, но требует миграции и затрагивает каждый вызов `update` — крупно и рискованно для узкой задачи; отклонено как несоразмерное.

  **Тесты при фиксе** (правило bug→regression): concurrent `markPayment(Paid,Cash)` ↔ `markPayment(Paid,Card)` через `collectAllPar` → ровно один победитель, поля платежа консистентны (метод/время — от победителя, не «торн»); сохранить идемпотентность re-pay (`paidAt` не перетирается); mutation-чек (откат CAS→update краснит тест); integration-тест на реальной БД (поля платежа реально записаны).

---

## Фича: клиентская компания ↔ несколько таксофирм (M:N) + выбор фирмы при создании поездки — 2026-06-22

> Контекст: сейчас `ClientCompany` жёстко привязана к одной таксофирме (`taxiCompanyId: CompanyId`), при создании поездки `companyId` берётся из JWT клиента, и через WS-фильтр (`event.companyId == dispatcher.companyId`, `api/.../routes/WebSocketRoutes.scala:110`) поездка падает диспетчерам только этой одной фирмы. Бизнес-требование: клиентская компания может сотрудничать с несколькими таксофирмами; клиент САМ выбирает фирму при создании поездки → поездка падает диспетчерам только выбранной фирмы. Решения подтверждены пользователем: (1) клиент выбирает фирму, (2) связь M:N через join-таблицу, (3) объём backend+Flutter. План: `~/.claude/plans/flutter-get-rides-019eee32-f6ad-7cf0-827-floating-stearns.md`. _Спроектировано по коду master, к реализации позже._

- [ ] **[FEATURE/HIGH] Миграция V11 — join-таблица партнёрства** _(спроектировано по коду)_ — новый `api/src/main/resources/db/migration/V11__Client_company_taxi_partners.sql`: таблица `client_company_taxi_companies(client_company_id, taxi_company_id, priority, created_at, PK(client_company_id, taxi_company_id))` с `ON DELETE CASCADE` на оба FK; индекс `idx_ccp_taxi_company(taxi_company_id)`; бэкфилл существующих 1:1 (`INSERT ... SELECT id, taxi_company_id FROM client_companies ON CONFLICT DO NOTHING`). Колонку `client_companies.taxi_company_id` НЕ дропать (остаётся «владельцем» для billing-изоляции `findByTaxiCompany`). V11 — следующий свободный номер (есть V1, V6–V10). _Категория: Schema._
- [ ] **[FEATURE/HIGH] Методы партнёрства на core `ClientCompanyRepository`** _(спроектировано по коду)_ — `core/.../repository/ClientCompanyRepository.scala` (+ Postgres + in-memory double в `core/src/test`): `addTaxiPartner(ccId, taxiId, priority=0)` (`INSERT ... ON CONFLICT DO UPDATE SET priority`), `removeTaxiPartner(...)→Boolean`, `listTaxiPartners(ccId)→List[CompanyId]` (`ORDER BY priority, created_at`), `isPartner(ccId, taxiId)→Boolean` (`EXISTS`, горячий путь валидации). ВАЖНО: методы именно на **core**-трейте, не billing — `build.sbt` имеет `billing dependsOn ride`, поэтому `ride` не может зависеть от `billing` (цикл); core доступен всем. _Категория: Functional._
- [ ] **[FEATURE/HIGH] Создание поездки: `companyId` из request + валидация партнёрства** _(спроектировано по коду)_ — (a) `ride/.../infrastructure/http/dto/RideApiModels.scala` — `CreateRideApiRequest` начать читать `companyId` (Flutter уже шлёт его в `toJson()`, бэкенд сейчас игнорирует). (b) `ride/.../openapi/RideApi.scala:254-270` (`createRideServer`) — заменить безусловный `requireCompanyId(user.companyId)`: для CLIENT/CLIENT_SECRETARY брать таксофирму из `apiRequest.companyId` (fallback на JWT при отсутствии — обратная совместимость); для staff (DISPATCHER/SECRETARY/DRIVER) оставить JWT-companyId, значение request игнорировать. (c) `ride/.../application/service/RideService.scala:115-138` (`createRide`) — если у клиента есть `clientCompanyId`, требовать `clientCompanyRepository.isPartner(clientCompanyId, request.companyId)`, иначе `BusinessRuleViolation("partner_isolation", ...)`; иначе сохранить текущую `client.companyId.contains(request.companyId)`. DI: `RideServiceImpl` + `layer` (`:88-98, 902-910`) получают core `ClientCompanyRepository`. Публикация `RideCreated(companyId=persistedRide.companyId)` — без изменений. _Категория: Functional/Security._
- [ ] **[FEATURE/INFO] Доставка диспетчерам — изменений НЕ требуется** _(проверено по коду)_ — `api/.../routes/WebSocketRoutes.scala:103-132` фильтр `event.companyId == dispatcher.companyId` уже доставит поездку только диспетчерам выбранной фирмы (т.к. `Ride.companyId` = выбранная фирма). `WebSocketEvent.RideCreated` без изменений. Проверить `notification/.../PushNotificationListener` (ветка `RideCreated`): если фанаут по `event.companyId` — корректно автоматически. _Категория: Functional._
- [ ] **[FEATURE/MEDIUM] API управления партнёрством** _(спроектировано по коду)_ — `billing/.../openapi/BillingClientCompanyApi.scala`: `GET/POST/DELETE /api/billing/companies/{id}/taxi-companies` (список/добавить/удалить партнёра). Авторизация (anti-injection): DISPATCHER/ADMIN может добавлять/удалять ТОЛЬКО свой `taxiCompanyId` (чтобы фирма B не вписала себя/фирму C в связи фирмы A), SuperAdmin — любой; переиспользовать ownership-проверку `BillingClientCompanyApi.scala:101-107`. Плюс клиентский эндпоинт `GET /api/client/taxi-companies` (для дропдауна) — партнёры своей компании по `user.clientCompanyId` строго из JWT, не из path; роли CLIENT/CLIENT_SECRETARY. Подключить новые server-endpoints в `api/.../Application.scala`. _Категория: Functional/Security._
- [ ] **[FEATURE/MEDIUM] Flutter: выбор таксофирмы в форме создания поездки** _(спроектировано по коду)_ — сервис `getMyTaxiCompanies()→GET /client/taxi-companies` + модель `TaxiPartner`; стейт `create_ride_form_state.dart` (`selectedTaxiCompanyId`, `availableTaxiCompanies`, gate `isValid` для CLIENT); события `TaxiCompanySelected`/`TaxiCompaniesLoaded` + обработчики в `create_ride_form_bloc.dart`; экран `create_ride_screen.dart` грузит партнёров на init (при одном — авто-выбор + скрыть дропдаун); UI `create_ride_form_sections.dart` (`DropdownButtonFormField`, только CLIENT/CLIENT_SECRETARY); `create_ride_form_helper.dart:118-142` — `companyId` из `state.selectedTaxiCompanyId` вместо `authState.user!.companyId`; синхронизировать `create_ride_request.dart` + `web/test/models/create_ride_request_test.dart`. _Категория: Functional._
- [ ] **[FEATURE/HIGH] Тесты** _(к написанию при реализации; правило fix→tests / bug→regression)_ — unit (in-memory, приоритет): `RideService.createRide` негатив `partner_isolation` (партнёры {A,B}, выбор C → отказ) + позитив (выбор A → `companyId=A`, событие `RideCreated(companyId=A)`) + staff-путь (диспетчер A для клиента A ок, для B отказ); repo-spec новых методов партнёрства (upsert `ON CONFLICT`, cascade, `isPartner`); integration (Testcontainers): Postgres-spec репозитория + проверка бэкфилла V11, e2e в `RideLifecycleIntegrationSpec.scala`; WS: `RideCreated(companyId=A)` приходит сокету A и НЕ приходит B; Flutter: bloc-тест на новые события + `isValid`, e2e в `web/integration_test/` (выбор не-партнёра → ошибка). _Категория: Tests._
- [ ] **[FEATURE/SECURITY] Риски тенант-изоляции — чеклист при реализации** _(спроектировано)_ — (1) `companyId` теперь из request → НЕ доверять: для staff брать из JWT, request игнорировать; для CLIENT — валидировать через `isPartner` (дропдаун это удобство, не граница безопасности; отказ жёсткий + лог как `RideService.scala:103`). (2) Эндпоинты партнёрства — POST/DELETE ограничить своим `taxiCompanyId` (+ SuperAdmin). (3) Клиентский список партнёров — `clientCompanyId` только из JWT. (4) `clientId` для CLIENT уже форсится в `user.userId` (route) — сохранить. (5) billing `findByTaxiCompany`/инвойсинг оставить на `taxi_company_id` (владелец), не путать с партнёрством для диспатча. _Категория: Security._
